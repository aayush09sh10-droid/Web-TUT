import React from 'react'
import { DARK_DOUBT_THEME, LIGHT_DOUBT_THEME } from '../../homeTheme'
import { hasStructuredDoubtContent } from '../../homeUtils'
import { setHomeField } from '../../store/homeSlice'
import { useAppDispatch, useAppSelector } from '../../../../store/hooks'

export default function AskDoubtFeature({ handleSubmitDoubt, hasLearningContext, onRegenerate }) {
  const dispatch = useAppDispatch()
  const isDarkMode = useAppSelector((state) => state.auth.theme === 'dark')
  const DOUBT_THEME = isDarkMode ? DARK_DOUBT_THEME : LIGHT_DOUBT_THEME
  const { doubtQuestion, doubtLoading, doubtError, result } = useAppSelector((state) => state.home)
  const doubt = result?.doubt
  const doubtAnswer = doubt?.answer

  return (
    <div className="rounded-[1.6rem] border p-4 shadow-[0_18px_40px_rgba(173,78,167,0.14)] sm:p-5" style={{ backgroundColor: DOUBT_THEME.surface, borderColor: DOUBT_THEME.primary, color: DOUBT_THEME.text, backgroundImage: isDarkMode ? `radial-gradient(circle at top left, rgba(255,182,246,0.1), transparent 28%), linear-gradient(135deg, ${DOUBT_THEME.surface}, rgba(38,16,42,0.98))` : `radial-gradient(circle at top left, rgba(255,255,255,0.82), transparent 32%), linear-gradient(135deg, ${DOUBT_THEME.surface}, #ffe8fb)` }}>
      <h3 className="text-lg font-semibold">Ask Your Doubt</h3>
      <p className="mt-2 text-sm leading-relaxed" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>{hasLearningContext ? 'Ask about concepts, numericals, formula-lab practice, or code. The answer will be split into concept, main explanation, conclusion, and a short real-life example.' : 'Paste a YouTube link or upload notes, then ask directly. We will prepare the learning context first and answer the doubt in a structured way.'}</p>
      <form className="mt-4 space-y-3" onSubmit={handleSubmitDoubt}>
        <textarea value={doubtQuestion} onChange={(e) => dispatch(setHomeField({ field: 'doubtQuestion', value: e.target.value }))} placeholder={hasLearningContext ? 'Ask your doubt here...' : 'Ask any concept, numerical, formula, or code doubt...'} rows={4} className="w-full rounded-[1.2rem] border px-4 py-3 text-sm outline-none" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.82)', borderColor: DOUBT_THEME.accent, color: DOUBT_THEME.text }} />
        <div className="flex flex-wrap gap-3">
          <button type="submit" disabled={doubtLoading} className="pressable-control w-auto rounded-full border px-4 py-2 text-xs font-semibold transition disabled:cursor-not-allowed disabled:opacity-60 sm:px-5 sm:py-2.5 sm:text-sm" style={{ background: `linear-gradient(135deg, ${DOUBT_THEME.primary}, ${DOUBT_THEME.secondary})`, borderColor: DOUBT_THEME.accent, color: DOUBT_THEME.text }}>
            {doubtLoading ? hasLearningContext ? 'Solving...' : 'Preparing...' : 'Ask Doubt'}
          </button>
          {(doubt?.question || doubtQuestion.trim()) && (
            <button type="button" onClick={onRegenerate} disabled={doubtLoading} className="pressable-control w-auto rounded-full border px-4 py-2 text-xs font-semibold transition disabled:cursor-not-allowed disabled:opacity-60 sm:px-5 sm:py-2.5 sm:text-sm" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.72)', borderColor: DOUBT_THEME.accent, color: DOUBT_THEME.text }}>
              {doubtLoading ? 'Regenerating...' : 'Regenerate'}
            </button>
          )}
        </div>
      </form>
      {doubtError && <p className="mt-4 rounded-xl border border-red-300 bg-red-50 px-4 py-2 text-sm text-red-700">{doubtError}</p>}
      {hasLearningContext && hasStructuredDoubtContent(doubtAnswer) && <div className="mt-5 rounded-[1.25rem] border px-4 py-4" style={{ background: isDarkMode ? DOUBT_THEME.card : 'rgba(255,255,255,0.6)', borderColor: DOUBT_THEME.accent }}>{doubt?.question && <p className="mb-3 text-sm font-medium" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Your doubt: {doubt.question}</p>}<h4 className="text-base font-semibold">{doubtAnswer.title || 'Teacher Explanation'}</h4>{doubtAnswer.concept && <div className="mt-4 rounded-xl border px-4 py-3 text-sm" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}><p className="text-xs font-semibold uppercase tracking-[0.12em]" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Concept</p><p className="mt-2 leading-relaxed">{doubtAnswer.concept}</p></div>}{(doubtAnswer.mainBody || doubtAnswer.explanation) && <div className="mt-3 rounded-xl border px-4 py-3 text-sm" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}><p className="text-xs font-semibold uppercase tracking-[0.12em]" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Main Body</p><p className="mt-2 whitespace-pre-wrap leading-relaxed">{doubtAnswer.mainBody || doubtAnswer.explanation}</p></div>}{Array.isArray(doubtAnswer.steps) && doubtAnswer.steps.length > 0 && <div className="mt-3 space-y-2">{doubtAnswer.steps.map((step, index) => <div key={`${step}-${index}`} className="rounded-xl border px-3 py-2 text-sm" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}>Step {index + 1}: {step}</div>)}</div>}{doubtAnswer.numerical?.isNumerical && <div className="mt-4 rounded-xl border px-4 py-4 text-sm" style={{ background: isDarkMode ? 'rgba(255,255,255,0.06)' : 'rgba(255,255,255,0.88)', borderColor: DOUBT_THEME.accent }}><p className="text-xs font-semibold uppercase tracking-[0.12em]" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Numerical Solution</p>{doubtAnswer.numerical.formulaUsed && <p className="mt-2 leading-relaxed">Formula used: {doubtAnswer.numerical.formulaUsed}</p>}{Array.isArray(doubtAnswer.numerical.knownValues) && doubtAnswer.numerical.knownValues.length > 0 && <div className="mt-3 flex flex-wrap gap-2">{doubtAnswer.numerical.knownValues.map((value) => <span key={value} className="rounded-full border px-3 py-1.5 text-xs" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}>{value}</span>)}</div>}{Array.isArray(doubtAnswer.numerical.steps) && doubtAnswer.numerical.steps.length > 0 && <div className="mt-3 space-y-2">{doubtAnswer.numerical.steps.map((step, index) => <div key={`${step.label}-${index}`} className="rounded-xl border px-3 py-3" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}><p className="font-semibold">{step.label}</p><p className="mt-1 leading-relaxed">{step.detail}</p></div>)}</div>}{doubtAnswer.numerical.finalAnswer && <p className="mt-3 rounded-xl border px-3 py-2 font-medium" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}>Final answer: {doubtAnswer.numerical.finalAnswer}</p>}</div>}{doubtAnswer.code?.snippet && <div className="mt-4 rounded-xl border px-4 py-4 text-sm" style={{ background: isDarkMode ? 'rgba(255,255,255,0.06)' : 'rgba(255,255,255,0.88)', borderColor: DOUBT_THEME.accent }}><div className="flex items-center justify-between gap-3"><p className="text-xs font-semibold uppercase tracking-[0.12em]" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Code Help</p>{doubtAnswer.code.language && <span className="rounded-full border px-3 py-1 text-[11px]" style={{ borderColor: DOUBT_THEME.accent }}>{doubtAnswer.code.language}</span>}</div><pre className="mt-3 overflow-x-auto rounded-xl border px-3 py-3 text-xs leading-6" style={{ background: isDarkMode ? 'rgba(14,10,20,0.78)' : 'rgba(250,245,255,0.98)', borderColor: DOUBT_THEME.accent }}><code>{doubtAnswer.code.snippet}</code></pre>{doubtAnswer.code.explanation && <p className="mt-3 leading-relaxed">{doubtAnswer.code.explanation}</p>}{Array.isArray(doubtAnswer.code.notes) && doubtAnswer.code.notes.length > 0 && <div className="mt-3 space-y-2">{doubtAnswer.code.notes.map((note, index) => <div key={`${note}-${index}`} className="rounded-xl border px-3 py-2" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}>{note}</div>)}</div>}</div>}{doubtAnswer.conclusion && <div className="mt-4 rounded-xl border px-4 py-3 text-sm" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}><p className="text-xs font-semibold uppercase tracking-[0.12em]" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Conclusion</p><p className="mt-2 leading-relaxed">{doubtAnswer.conclusion}</p></div>}{doubtAnswer.realLifeExample && <div className="mt-3 rounded-xl border px-4 py-3 text-sm" style={{ background: isDarkMode ? DOUBT_THEME.cardSoft : 'rgba(255,255,255,0.8)', borderColor: DOUBT_THEME.accent }}><p className="text-xs font-semibold uppercase tracking-[0.12em]" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Real Life Example</p><p className="mt-2 leading-relaxed">{doubtAnswer.realLifeExample}</p></div>}{doubtAnswer.keyTakeaway && <p className="mt-3 text-sm font-medium" style={{ color: isDarkMode ? DOUBT_THEME.muted : '#7d3d74' }}>Key takeaway: {doubtAnswer.keyTakeaway}</p>}</div>}
    </div>
  )
}
